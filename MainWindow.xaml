<Window x:Class="Baza.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Baza"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        ResizeMode="NoResize"
        WindowStyle="ThreeDBorderWindow">

    <StackPanel>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem x:Name="Connect" Header="_Login" InputGestureText="Ctrl+C" Click="Connect_Click">
                        <MenuItem.Icon>
                            <Image Source="./Resources/connect.ico" Height="25" Width="25" />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="Exit" Header="_Exit" InputGestureText="Ctrl+E" Click="Exit_Click">
                        <MenuItem.Icon>
                            <Image Source="./Resources/exit.jpg" Height="25" Width="25"  />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
            </Menu>
        </DockPanel>
        <Grid>
            <TabControl Height="400">

                <TabItem Header="Klienci">
                    <StackPanel>
                        <Grid Margin="10" Height="340">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="450"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <DataGrid Grid.Column="0" Name="klienciGrid" AutoGenerateColumns="True" SelectionChanged="klienci_SelectionChanged" />

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10 0 0 0">
                                <TextBlock>Imię:</TextBlock>
                                <TextBox x:Name="nameTextBox" Text="{Binding Path=CurrentItem}"/>
                                <TextBlock>Nazwisko:</TextBlock>
                                <TextBox x:Name="surnameTextBox" MaxLength="11"/>
                                <TextBlock>Pesel:</TextBlock>
                                <TextBox x:Name="peselTextBox"/>
                                <Button Click="addClient_click" Margin="0 10 0 0">Zapisz</Button>
                                <Button Click="deleteClient_Click">Kasuj</Button>
                            </StackPanel>

                        </Grid>
                    </StackPanel>
                </TabItem>

                <TabItem Header="Pracownicy">
                    <StackPanel>
                        <Grid Margin="10" Height="250">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="233*"/>
                                <RowDefinition Height="33*"/>
                                <RowDefinition Height="4*"/>
                            </Grid.RowDefinitions>

                            <DataGrid Name="pracownicyGrid" AutoGenerateColumns="True" Margin="0,0,0,-41" Grid.RowSpan="3" SelectionChanged="g1_SelectionChanged" />

                        </Grid>
                    </StackPanel>
                </TabItem>

                <TabItem Header="Samochody">
                    <StackPanel>
                        <Grid Margin="10" Height="250">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="233*"/>
                                <RowDefinition Height="33*"/>
                                <RowDefinition Height="4*"/>
                            </Grid.RowDefinitions>

                            <DataGrid Name="samochodyGrid" AutoGenerateColumns="True" Margin="0,0,0,-41" Grid.RowSpan="3" SelectionChanged="g1_SelectionChanged" />

                        </Grid>
                    </StackPanel>
                </TabItem>

                <TabItem Header="Wypozyczenia">

                    <StackPanel>
                        <Grid Margin="10" Height="340">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="450"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <DataGrid Grid.Column="0"  Name="wypozyczeniGrid" AutoGenerateColumns="True"  SelectionChanged="g1_SelectionChanged" />

                            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10 0 0 0">
                                <TextBlock>SamID:</TextBlock>
                                <TextBox x:Name="samIDTextBox" Text="{Binding Path=CurrentItem}"/>
                                <TextBlock>pracID:</TextBlock>
                                <TextBox x:Name="pracIDTextBox"/>
                                <TextBlock>KlientID:</TextBlock>
                                <TextBox x:Name="klientIDTextBox"/>
                                <TextBlock x:Name="dataWypozyczeniaDatePicker">Data wypożyczenia:</TextBlock>
                                <DatePicker></DatePicker>
                                <TextBlock>Data zwrotu:</TextBlock>
                                <DatePicker x:Name="dataZwrotuDatePicker"></DatePicker>
                                <TextBlock>Koszt</TextBlock>
                                <TextBox x:Name="koszTextBox"/>
                                <Button Click="addWypozyczenie" Margin="0 10 0 0">Zapisz</Button>
                                <Button Click="deleteWypozyczenie">Kasuj</Button>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                </TabItem>

                <TabItem Header="Raporty">

                    <StackPanel>
                        <Grid Margin="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="5*" />
                            </Grid.ColumnDefinitions>

                            <Button Name="Start" Content="Start" Grid.Column="0" Margin="0 0 10 0" Click="Start_Click"/>
                            <Label Content="zapytanie: " Grid.Column="1"/>
                            <TextBox Grid.Column="2" Name="inquiryTextBox"/>
                        </Grid>

                        <Grid Margin="10" Height="250">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="450"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <DataGrid Grid.Column="0" Name="raportyGrid" AutoGenerateColumns="True" Margin="0,0,0,-41" Grid.RowSpan="3" SelectionChanged="g1_SelectionChanged" />

                            <StackPanel Grid.Column="1">
                                <Button Click="topClient_Click">Top Klienci</Button>
                                <Button>Top Samochody</Button>
                                <Button>Top Pracownicy</Button>
                            </StackPanel>
                            
                        </Grid>

                    </StackPanel>

                </TabItem>

                <TabItem Header="Historia">
                    <StackPanel>
                        <Grid Margin="10" Height="250">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="233*"/>
                                <RowDefinition Height="33*"/>
                                <RowDefinition Height="4*"/>
                            </Grid.RowDefinitions>

                            <DataGrid Name="historiaGrid" AutoGenerateColumns="True" Margin="0,0,0,-41" Grid.RowSpan="3" SelectionChanged="g1_SelectionChanged" />
                        </Grid>
                    </StackPanel>

                </TabItem>


            </TabControl>
        </Grid>

    </StackPanel>
</Window>
